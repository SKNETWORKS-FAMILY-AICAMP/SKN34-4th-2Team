rules_version = '2';
// ⚠️ 초기 시드 전용 — 시드 완료 후 firestore.rules 로 복원하세요!
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
