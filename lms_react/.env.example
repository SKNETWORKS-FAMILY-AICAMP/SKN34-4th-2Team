# 로컬(Vite dev): /api → vite.config 프록시 → Django :8000
# 배포(같은 도메인 + Nginx /api 프록시): /api 그대로 쓰거나 비워도 기본 /api
VITE_API_BASE=/api

# API가 다른 호스트일 때만 (예: CloudFront 웹 + api.example.com)
# VITE_API_BASE=https://api.example.com/api
