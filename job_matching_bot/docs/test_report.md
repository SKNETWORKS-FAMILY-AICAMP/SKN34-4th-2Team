# 테스트 계획 및 결과 보고서

채용공고 추천봇을 무엇으로, 어떻게 쟀는지와 그 결과를 적는다. 구조는
[architecture.md](architecture.md), 데이터는 [data_preprocessing.md](data_preprocessing.md)에 있다.

## 1. 무엇을 검증하나

추천봇의 약속은 셋이다. 테스트도 이 셋을 기준으로 짠다.

| 약속 | 깨지면 | 무엇으로 재나 |
|---|---|---|
| **조건에 맞는 공고만 낸다** | 신입에게 경력 5년 공고, 서울 희망자에게 부산 공고 | 단위 테스트, 규칙 결함 검사 |
| **근거는 원문 인용이다** | 이력서에 없는 경험을 근거로 댄다 | 근거 검증 코드, 규칙 결함 검사, 챗봇 사람 채점 |
| **맞는 공고를 고른다** | 조건은 맞는데 직무가 엉뚱하다 | 사람 채점 추천 평가 |

앞의 둘은 규칙으로 판정할 수 있어 자동으로 잰다. 마지막은 "이 공고가 이 사람에게
맞나"라서 정답이 없고, 사람이 매긴다. **모델이 낸 등급을 정답으로 쓰지 않는다** — 자기
답안을 자기가 채점하는 셈이라 아무것도 검증하지 못한다.

## 2. 테스트 계획

| 층 | 도구 | 외부 호출 | 언제 |
|---|---|---|---|
| ① 단위 테스트 | `tests/` (unittest) | 없음 | 커밋 전마다 |
| ② 규칙 결함 검사 | `evaluation/recommend_check.py` | 추천 API | 필터·프롬프트를 고친 전후 |
| ③ 추천 사람 채점 | `evaluation/recommend_eval.py` | 추천 API | 프롬프트·가중치·표시 건수를 바꾼 뒤 |
| ④ 챗봇 평가 | `evaluation/chat_eval.py` | 라우터는 LLM만, 서버층은 API | 챗봇 프롬프트·라우터를 고친 뒤 |
| ⑤ 적재 파이프라인 | 야간 배치 리포트, `runs` 표 | 사람인·OpenAI·Pinecone | 매일 밤 자동 |

### ① 단위 테스트

```powershell
python -m unittest discover -s job_matching_bot/tests -t .
```

외부 접속 없이 돈다. OpenAI·Pinecone·사람인 호출은 가짜 객체로 갈아끼운다.
테스트 파일 35개가 다루는 것:

| 영역 | 테스트 |
|---|---|
| 수집 | 상세 파싱, 마감 공고 건너뛰기, 주석으로 끊긴 글자 복원, 상세 수집 순서, 야간 배치 |
| 정제 | 요건 구간 분리, 전공·자격증 추출, 목록 조건 파싱, 이미지 본문 판정, 기술 태그 분리 |
| 저장·인덱싱 | SQLite 저장소, 증분 판정, `embed_hash`, 공유 파일 |
| 검색·매칭 | 게시 중 공고만 검색, 전국 근무 지역, 표기 흔들림, 기술 겹침 재정렬, 기술명 정규화 |
| API | 추천 스트리밍, 챗봇 엔드포인트, 챗봇 라우팅, 욕설 차단, 마감 확인 |
| 평가 도구 | 채점 페이지, 규칙 결함 검사, 챗봇 케이스가 안 보는 칸이 없는지 |

### ② 규칙 결함 검사

사람 없이도 **확실히 틀렸다고 말할 수 있는 것**만 센다. 취향 문제는 넣지 않는다.

| 검사 | 결함 |
|---|---|
| 신입에게 경력 공고 | 경력 0년 이력서에 경력자 채용 공고 |
| 희망 지역 밖 | 희망 지역과 겹치지 않고 전국 근무도 아닌 공고 |
| 희망 고용형태 밖 | 정규직만 원하는데 계약직 |
| 근거 없는 "높음" | 인용이 하나도 없는데 적합도 높음 |
| 원문에 없는 인용 | `job_quote`가 공고 원문에 없다 |
| 마감·삭제된 공고 | 저장소 상태가 CLOSED/REMOVED/EXPIRED |
| 저장소에 없는 공고 | 인덱스와 저장소가 어긋났다 |
| 회사 상한 초과 | 한 회사가 3건 이상 |

결함이 하나라도 있으면 종료 코드 1을 돌려준다.

```powershell
python -m job_matching_bot.evaluation.recommend_check --save                  # 앱 목업 5종
python -m job_matching_bot.evaluation.recommend_check --save --eval-resumes    # 평가 전용 5종
```

`--save`는 건수를 `fixtures/checks/`에 남기고, 추천 원본에는 **받은 날과 그때의 이력서 조건**을
같이 저장한다. 나중에 원본으로 다시 검사할 때 이력서 조건은 저장된 것을 쓰고, 마감 검사는 받은
날에만 한다.

### ③ 추천 사람 채점

```powershell
python -m job_matching_bot.evaluation.recommend_eval --run --top-k 12 --eval-resumes
# 채점 페이지에서 1/2/3 키로 매기고 CSV를 내려받아 fixtures/eval_labels.csv로 옮긴다
python -m job_matching_bot.evaluation.recommend_eval --score --eval-resumes
```

| 키 | 기준 |
|---|---|
| `1` 추천·높음 | 직무가 같고 주된 기술이 겹친다 |
| `2` 추천·보통 | 직무는 같은데 주된 기술이 다르다 |
| `3` 추천 안 함 | 지원을 권하지 않는다 |

채점 원칙:

- **모델 등급을 가린 채 먼저 매긴다.** 채점 페이지에서 모델의 등급과 근거는 접혀 있다.
- **고친 근거가 된 이력서로 다시 재지 않는다.** 그 이력서에 맞춘 셈이라 늘 좋아 보인다.
  회차마다 이력서 묶음을 바꾼다(앱 목업 5종 → 평가 전용 5종).
- **표본이 작다는 것을 숫자와 함께 적는다.** 50건에서 오추천율 20%의 95% 구간은 대략 9~39%다.
  큰 변화만 말한다.
- 회차마다 결과를 `fixtures/labels/`에 한 벌씩 남기고 지우지 않는다.

| 지표 | 정의 |
|---|---|
| 상위 6건 오추천율 | 앱이 보여 주는 6건 중 사람이 "추천 안 함"으로 매긴 비율. **사용자가 실제로 보는 것** |
| 전체 오추천율 | 판정한 12건 전체에서의 비율 |
| 등급 일치율 | 사람이 추천한 공고 중 모델 등급(높음/보통)이 사람과 같은 비율 |
| 등급별 정확도 | 모델이 그 등급을 준 공고 중 사람이 추천한 비율 |

### ④ 챗봇 평가

챗봇은 대부분의 지표에 정답이 있다("서울 백엔드 신입" → 지역 서울·직무 백엔드·신입).
그래서 셋으로 나눈다.

```powershell
python -m job_matching_bot.evaluation.chat_eval --router   # 말을 가른 결과 대조. 서버 불필요
python -m job_matching_bot.evaluation.chat_eval --run      # 앱이 받는 응답 대조. 서버 필요
python -m job_matching_bot.evaluation.chat_eval --sheet    # 답 문장만 사람이 매길 페이지
python -m job_matching_bot.evaluation.chat_eval --score
```

라우터를 따로 재는 이유는 **응답에 안 실리는 칸**(`intent`, `job_refs`, `unavailable` 등)이
있어서다. 케이스는 `fixtures/chat_cases.json`에 48개, 62턴이 있다.

| 갈래 | 케이스 | 예 |
|---|---:|---|
| 검색 · 조건 이어가기 · 연차 | 11 | "백엔드 공고 보여줘" → "서울만", "3년차인데 갈 만한 데 있어?" |
| 번호 지목 · 직전 답 가리키기 | 7 | (목록 뒤) "1번하고 3번 비교해줘", 목록 없이 "2번 알려줘" |
| 질문 | 6 | "백엔드 신입은 어떤 기술을 많이 요구해?", "취업 너무 막막해" |
| 범위 밖 · 욕설 · 인사 | 7 | "면접 준비하게 파이썬 코드 짜줘", "호구", "안녕" |
| 못 하는 요청 | 3 | "연봉 높은 순으로 보여줘", "나 여기 붙을 확률 얼마야?" |
| 이력서 기반 추천 | 2 | "내 프로젝트 경험만 보고 추천해줘" |

사람은 답 문장만 매긴다.

| 라벨 | 뜻 |
|---|---|
| 근거 있음 | 실제 공고나 건수를 근거로 답했다 |
| 일반론 | 공고로 답했어야 하는데 어디서나 들을 수 있는 말을 했다 |
| 해당 없음 | 공고를 근거로 댈 물음이 아니다(인사, 못 하는 요청, 범위 밖) |
| 지어냄 | 없는 공고·조건을 말했다 |

## 3. 결과

### ① 단위 테스트 — 2026-09-13

| 실행 위치 | 실행 | 통과 | 건너뜀 | 실패 | 시간 |
|---|---:|---:|---:|---:|---:|
| 개발 폴더(수집 데이터·Dart 정답지 있음) | 646 | **646** | 0 | 0 | 366초 |
| 다른 이름의 새 체크아웃 (한계 보완 전) | 620 | 609 | 10 | 1 | 3초 |

- 새 체크아웃에서 **건너뛴 10건**은 앱(Dart)과 같은 규칙으로 이력서를 읽는지 대조하는 테스트다.
  Dart 쪽 정답지를 `flutter test`로 먼저 만들어야 돈다.
- 새 체크아웃의 **실패 1건**은 `test_env`가 레포 폴더 이름을 `SKN34-3rd-2Team`으로 못 박아 둔 탓이었다.
  기능 결함이 아니라 테스트의 가정 문제라, 폴더 이름 대신 레포 루트에 앱과 이 패키지가 함께 있는지 보도록 고쳤다.
- 늘어난 26건은 추천·챗봇 단계별 시간, 요건 구간 글에서 기술 찾기, 결함 검사의 이력서 조건 보존, 챗봇 "이거 말고"·키워드·좁은 지역·마감 시각, 야간 배치 파일 이름을 지키는 테스트다(쓰지 않게 된 기능의 테스트 5건은 뺐다).

### ② 규칙 결함 검사 — 2026-09-13

| 이력서 묶음 | 이력서 | 검사한 공고 | 결함 |
|---|---:|---:|---:|
| 앱 목업 | 5 | 30 | **0** |
| 평가 전용 | 5 | 26 | **0** |

여덟 가지 검사 모두 0건이다. 평가 전용 묶음이 30건이 아니라 26건인 것은 "DevOps 경력 2년" 이력서가
조건(서울·정규직·경력 2년)을 통과한 공고가 2건뿐이었기 때문이다. 기록은 `fixtures/checks/`에 회차마다 남는다.

### ③ 추천 사람 채점 — 2026-09-11

| 회차 | 이력서 | 건수 | 상위 6건 오추천율 | 전체 오추천율 | 등급 일치율 |
|---|---|---:|---:|---:|---:|
| 1차 (새벽) | 앱 목업 5종 | 43 | 10% (3/30) | 20.9% (9/43) | 55.9% |
| 2차 (오후) | 평가 전용 5종 | 50 | **7.7% (2/26)** | 22.0% (11/50) | **74.4% (29/39)** |

1차 뒤 판정 프롬프트를 고쳤고, 2차는 **고칠 때 보지 않은 이력서**로 쟀다.

등급별 정확도(모델이 그 등급을 준 공고 중 사람이 추천한 비율):

| 모델 등급 | 1차 | 2차 |
|---|---:|---:|
| 높음 | 16/16 (100%) | 10/11 (91%) |
| 보통 | 17/25 (68%) | 23/25 (92%) |
| 낮음 | 1/2 (50%) | 6/14 (43%) |

읽는 법:

- **사용자가 보는 6건의 오추천은 26건 중 2건이다.** 전체 오추천율(22%)이 높은 것은 7~12위에서
  나온다. 그래서 앱에는 6건만 보여 준다.
- **"높음"과 "보통"은 믿을 만하다(91~92%).** 1차에서 68%였던 "보통"이 92%로 올랐다.
- **"낮음"을 빼면 안 된다.** 2차에서 "낮음" 14건 중 6건(43%)을 사람이 추천했다. "낮음은 숨기자"고 하면 좋은 공고를 같이 버린다.
- **검색은 병목이 아니다.** 사람이 추천한 공고가 벡터 검색 1~21위에 고르게 퍼져 있었다. 맞는 공고는 후보 25건 안에 들어와 있고, 문제는 그 안에서 고르는 순서다.
- 두 회차는 이력서가 달라서 숫자를 바로 빼서 비교하지 않는다. 근거 있는 말은 "크게 나빠지지 않았고, 등급 일치율은 올랐다"까지다.

사람이 추천하지 않은 2차 11건은 AI 연구 석사 4건, QA 3건, 정보보안 3건, DevOps 1건이다.
"컴퓨터비전 석사에 강화학습·로봇 연구 공고", "정보보안 신입에 백엔드 개발 공고"처럼 **직무가 인접한 공고**가
대부분이었다. 11건 중 8건은 모델도 "낮음"을 줬다.

### ③-2 사전 순위 비교 (3차) — 2026-09-13

태그가 없는 공고를 요건 구간 글에서 기술을 찾아 줄 세우는 변경(신)과 태그만 보던 예전 방식(구)을
**같은 이력서(평가 전용 5종)로 같은 시각에** 돌렸다. 구조화는 한 번만 받아 양쪽에 똑같이 넣었다.
두 쪽에 나온 51건 중 34건은 2차 라벨을 그대로 쓰고 17건을 새로 매겼다. 채점 페이지는 어느 쪽 결과인지
가렸다(`fixtures/labels/20260913-ab-eval-resumes.csv`).

| | 상위 6건 오추천율 | 전체 오추천율 | 등급 일치율 |
|---|---:|---:|---:|
| 신 (지금 코드) | **7.7% (2/26)** | 16% (8/50) | 78.6% (33/42) |
| 구 (태그만) | 15.4% (4/26) | 16% (8/50) | 64.3% (27/42) |

| 모델 등급 | 신 | 구 |
|---|---:|---:|
| 높음 | 12/13 (92%) | 13/15 (87%) |
| 보통 | 26/26 (100%) | 20/20 (100%) |
| 낮음 | 4/11 (36%) | 9/15 (60%) |

읽는 법:

- **이 차이는 사전 순위 변경의 효과가 아니다.** LLM이 판정한 12건은 양쪽이 50건 중 49건 같았다. 조건을
  통과한 후보가 대부분 12~13건이라 순서를 바꿔도 잘려 나가는 공고가 거의 없다.
- **차이를 만든 것은 LLM 판정의 흔들림이다.** 같은 공고를 몇 분 간격으로 판정했는데 49건 중 6건의 등급이
  달랐다. 상위 6건이 달라진 여덟 자리를 하나씩 보면 전부 한쪽에서 등급이 바뀐 공고이거나 그 때문에 밀린
  공고였다. 등급 일치율이 14%p 차이 나는 것도 같은 공고의 등급이 달라서다.
- 그래서 근거 있는 말은 **"사전 순위 변경으로 나빠지지 않았다"** 까지다. 좋아졌다고 말할 근거는 없다.
- 오히려 **같은 입력에 등급이 12% 흔들린다**는 것이 새로 알게 된 사실이다. 회차 간 오추천율 몇 %p 차이는
  이 흔들림 안에 들어간다.

### ④ 챗봇 평가 — 2026-09-11

| 층 | 결과 |
|---|---|
| 라우터 대조 (`--router`) | **36/36** 통과 |
| 서버 응답 대조 (`--run`) | **36/36** 통과 |

사람 채점(35건):

| 라벨 | 건수 |
|---|---:|
| 근거 있음 | 19 |
| 일반론 | 9 |
| 해당 없음 | 7 |
| 지어냄 | 1 |

- 도구가 계산한 근거율은 **68%(19/28)** 였다. 일반론 9건을 다시 읽어 보니 전부 연봉·합격 확률 같은
  **못 하는 요청**이나 코드 작성 같은 **범위 밖** 물음이었다. 공고로 답할 물음이 아니므로 "해당 없음"이
  맞고, 그렇게 세면 근거율은 **19/19**다. 이후로는 근거율 모수에서 "해당 없음"을 뺀다.
- **지어냄 1건**은 "3년차인데 갈 만한 데 있어?"에 경력 5년 이상 공고가 나간 것이다. 연차를 검색 조건으로
  쓰지 않고 있었다. 고친 뒤(`24b82db`) 이 물음을 케이스에 더했고, 위의 자동 대조 36/36에 들어 있다.
  답 문장 사람 채점은 고친 뒤 다시 하지 않았다.
- 자동 대조가 전부 통과해도 이 결함은 사람이 읽고서야 나왔다. 케이스에 없던 물음이었기 때문이다.

### ④-2 챗봇 다시 재기 — 2026-09-13

| 층 | 결과 | 응답 시간 중앙값 |
|---|---|---:|
| 라우터 대조 3회 | 3회 모두 **36/36** | 1.5초 |
| 서버 응답 대조 | **36/36** | 2.2초 (최대 11.1초) |

- **갈래별 시간.** 검색 2.9초(17턴), 질문 6.0초(7턴), 공고 5.2초(2턴), 비교 11.1초(1턴). 답을 쓰는
  갈래는 두 번째 LLM 호출이 대부분이다. 자세한 표는 [chatbot.md](chatbot.md)의 12장.
- **라우터 흔들림.** 같은 43턴을 세 번 가르고 모든 칸을 비교하니 4턴(9%)에서 한 칸 이상 달랐다.
  둘은 주제 문에서 먼저 막혀 답이 같고, 둘("30번 알려줘", "나 여기 붙을 확률 얼마야?")은 답이 달라질
  수 있는데 **케이스가 그 칸을 안 본다.** 36/36은 검사하는 칸이 고정됐다는 뜻이지 출력 전체가
  같다는 뜻이 아니다.
- **"이거 말고" 결함.** 사람 채점을 준비하다 "이거 말고 다른 거 보여줘"에 앞과 **같은 5건**을
  보여 주면서 "기존 공고는 제외하고 다른 공고를 찾았다"고 답하는 것을 찾았다. 케이스에 없던 말이다.
  앱이 같은 조건으로 본 공고를 모아 보내고 서버가 빼고 다음 공고를 주도록 고쳤고, 두 번 넘겨도
  겹치지 않는지 보는 케이스를 더했다. 고친 뒤 38/38(라우터·서버 모두).

### ④-3 챗봇 답 문장 사람 채점 2차 — 2026-09-14

기존 케이스 36턴의 마지막 답과 **케이스에 없던 새 물음 10개**를 매겼다(`fixtures/labels/chat-20260913-231129.csv`).

| | 근거 있음 | 일반론 | 해당 없음 | 근거율 | 지어냄 |
|---|---:|---:|---:|---:|---:|
| 1차 (09-11, 35건) | 19 | 9 → 해당 없음으로 다시 셈 | 7 | 19/19 | **1** |
| 2차 기존 케이스 36건 | 15 | 0 | 21 | **15/15** | **0** |
| 2차 새 물음 10건 | 6 | 2 | 2 | 6/8 | 0 |

- 1차의 지어냄(연차)은 다시 나오지 않았다.
- 새 물음의 일반론 2건("공기업 IT", "재택 QA")은 **키워드가 직무와 OR로 묶인 검색 결함**이었다. 3,000건
  넘게 걸렸다. 키워드는 직무와 함께 맞아야 걸리게 고쳤다 — 재택 QA는 27건으로 좁혀졌다.
- 채점 메모로 고친 것: 판교가 경기 전체로 넓어짐 → 시·군·구로 찾기, 자정 넘어 본 "오늘 23:59 마감"
  공고 → 보내기 직전에 마감 시각까지 보고 조기 마감 확인 캐시를 하루에서 한 시간으로, "적혀 있지
  않아요" → "공고 본문에서 확인이 안 돼요. 원문에서 확인해 주세요", 질문 답의 근거 공고 떼기.
- 고친 뒤 자동 대조는 38/38(라우터·서버)이고, 사람 재채점은 하지 않았다.

### ④-4 빼는 조건·새로 올라온 공고 — 2026-09-14

평가 케이스에 없는 물음 16개로 돌려 보다 찾았다. 라우터의 첫 줄이 실제로 걸지 않은 조건을 말했다("스타트업을 제외하고",
"오늘 새로 등록된"). 빼는 조건·올라온 날 칸을 더하고 라우터 지시문에 예시를 넣었다("경력 무관 신입 가능" → 신입,
"AI 공고 많아?" → 직무 AI). 검색 답에서 라우터 문장을 뺐다. 케이스 8개를 더했다.

| 층 | 고치기 전(38개) | 고친 뒤(46개) |
|---|---|---|
| 라우터 대조 | 38/38 | **46/46 · 46/46 · 46/46** (3회) |
| 서버 응답 대조 | 38/38 | **46/46** |

| 물음 | 고치기 전 | 고친 뒤 |
|---|---|---|
| 스타트업은 빼고 데이터 분석 신입 | 답만 "제외하고", 실제로 안 뺌 | 스타트업 제외 523건 |
| 오늘 새로 올라온 개발 공고 | 날짜로 안 거름(전체) | 마지막 수집에서 새로 본 공고 |
| 경력 무관 신입 가능한 QA | 경력 무관으로 뽑혀 경력 5년 공고 섞임 | 신입 |
| 요즘 AI 공고 많아? | "몇 건인지 알 수 없어요" | AI 공고 7,131건, 신입 361건 |
| 헤드헌팅 말고 백엔드 → 서울만 | — | 두 번째 말에도 헤드헌팅 제외 유지 |
| 대기업 IT 신입 → 서울만 → 데이터 분석가 되려면 뭐 준비해야 해? | 서울·대기업·신입이 붙어 24건 표로 답함 | 데이터분석만, 1,472건 |

앱에서 쓰다가 마지막 줄을 찾아 라우터 지시문에 "새 주제 질문은 직전 조건을 이어받지 않는다"를 더하고 케이스 2개를 넣었다.
라우터 대조 **48/48 · 48/48 · 48/48**, 서버 응답 대조 **48/48**.

### ⑤ 적재 파이프라인 — 최근 7회

| 적재일 | 신규 | 만료 | 삭제 | 적재 벡터 | 오류 |
|---|---:|---:|---:|---:|---|
| 09-08 | 4,615 | 1,327 | 0 | 3,653 | 없음 |
| 09-09 | 4,412 | 1,666 | 129 | 3,156 | 없음 |
| 09-10 | 4,242 | 2,046 | 377 | 3,095 | 없음 |
| 09-11 | 4,658 | 2,311 | 511 | 3,401 | 없음 |
| 09-12 | 5,668 | 4,703 | 776 | 4,216 | 없음 |
| 09-13 | 3,138 | 4,870 | 1,482 | 2,415 | 없음 |
| 09-14 | 3,814 | 5,089 | 1,576 | 2,818 | 없음 |

`적재 벡터`는 그날 새로 임베딩한 건수다. 2026-09-13 야간 배치는 383분 동안 목록 14개 대분류를 차단 없이
끝까지 훑었고, 상세 3,138건을 실패 0건으로 받았으며, 적재 종료 코드 0, 공유 파일 업로드 성공이었다.
인덱스 대상 23,627건 중 21,212건은 지문이 같아 **임베딩 비용 없이 건너뛰었다.**

### 응답 시간

2026-09-13, 위 ② 검사에서 받은 추천 10회의 서버 로그(`[추천 시간]`)다. 이력서는 매번 서버가
구조화했다(앱이 미리 보내지 않음). 첫 요청은 서버를 띄운 직후라 36.4초였고, 아래는 나머지 9회다.

| 단계 | 중앙값 | 범위 | 참고 |
|---|---:|---:|---|
| ① 이력서 구조화(LLM) | 2.8초 | 2.5~3.9 | 앱이 저장할 때 미리 보내면 0초 |
| ② 벡터 검색 | 1.6초 | 1.5~2.7 | 질의문 임베딩 포함 |
| ③ 하드 필터 | 0.0초 | — | 저장소 조회 포함 |
| ④ 마감 확인 | 2.6초 | 1.0~4.2 | 후보 공고 페이지를 동시에 연다. 확인 결과를 믿는 시간은 잴 때 하루였고 지금은 한 시간이다 |
| ⑤ 사전 순위 | 0.0초 | — | 글에서 기술 찾기 포함 |
| ⑥ LLM 재정렬 | **11.0초** | 8.0~15.3 | 12건 병렬, 가장 느린 한 건만큼 기다린다 |
| ⑦ 근거 검증 | 0.0초 | — | |
| **합계** | **18.6초** | 14.4~22.8 | 스트리밍으로 단계 진행을 보여 준다 |

- **전체의 60%가 재정렬이다.** 줄이려면 재정렬 건수·모델·추론 강도를 바꿔야 하는데, 셋 다 적합도에 영향이 있어 사람 채점과 함께 봐야 한다.
- **마감 확인이 생각보다 길다(2.6초).** 조건을 통과한 후보의 공고 페이지를 여는 시간이다. 지금은 재정렬 전에 끝나기를 기다린다.
- 예전 문서의 "10초대"는 기록 없이 눈으로 본 값이었고, 실측과 맞지 않았다.
- 재정렬 6건 → 12건의 시간 차이는 이번에 재지 않았다. 챗봇 시간은 ④-2에 있다.

## 4. 트러블슈팅

평가로 찾아서 고친 것들이다. 앞의 숫자들이 이 과정에서 나왔다.

| 문제 | 어떻게 알았나 | 원인 | 고친 것 | 결과 |
|---|---|---|---|---|
| 모든 공고의 유사도가 0.37~0.50에 뭉침 | 검색 점수 분포 | 임베딩 텍스트 앞의 분류 경로 줄이 모든 공고를 비슷하게 만듦 | 요건 구간만 임베딩 | 검색이 공고를 구분 |
| 적합도가 전부 "보통" | 판정 분포 | 메타데이터 원문을 300자로 잘라 자격요건이 빠짐 | 1,200자로 늘림(공고 88%가 온전히 들어감) | 높음·보통·낮음이 갈림 |
| 신입 이력서에 경력 공고가 섞여 나옴 | 규칙 결함 검사(09-08, 실행 결과는 남기지 않음) | 조건을 임베딩 유사도에 맡김 | 하드 필터를 LLM 앞에 둠, 검사를 재실행 가능한 도구로 남김 | 조건은 규칙으로 먼저 자르고 LLM은 그 안에서만 고름 |
| 누구를 LLM에 보낼지 순서가 무의미 | 후보 유사도 폭 0.042~0.140 | 벡터 순위가 후보 안에서 평평 | 기술 겹침과 반씩 섞음 | 판정과의 순위상관 +0.26 → +0.42 |
| 응답을 빠르게 한 뒤 적합도가 후해짐 | 판정 분포를 뒤늦게 비교("높음"이 회차마다 늘어남, 수치는 남기지 않음) | 눈으로만 확인 | 사람 채점 도구 도입 | 이후 변경은 숫자로 비교 |
| `JavaScript`가 기술로 안 잡힘 | 규칙 검사의 "원문에 없는 인용" | 사람인이 `script` 글자에 HTML 주석을 끼움 → `ja/vasc/ript`로 쪼개짐 | 주석을 먼저 지우고 텍스트 추출 | 기술 매칭·인용 검증 복구 |
| 자격요건이 빈 공고 57건(표본 4,000건) | 요건 추출 누락 조사 | 표 머리글이 먼저 몰려 나와 구간이 비어 보임 | 머리글을 각 칸 앞으로 옮김 | 하드 필터가 전공·자격증 조건을 읽음 |
| 챗봇 케이스 22건이 검사 없이 통과로 셈 | 케이스를 다시 읽음 | 응답에 없는 칸은 검사가 건너뜀 | 라우터 대조 층 분리, 안 보는 칸이 있으면 테스트가 깨지게 함 | 36/36이 실제로 본 결과 |
| 목록에서 안 보인 공고를 지우면 살아 있는 공고까지 삭제 | 12건을 열어 봄 → 8건 살아 있음 | 목록 노출은 마감 신호가 아님 | 이틀 연속 안 보이면 상세 페이지를 열어 확인 후 삭제 | 잘못 지우는 공고 방지 |
| 경력 3년 이력서에 "신입에게 경력 공고" 4건 | 지난 결과 파일을 다시 검사 | 결과 파일의 이력서 이름이 지금 목업과 달라 못 찾음 → 연차 0으로 봄 | 받을 때의 이력서 조건을 결과와 같이 저장, 못 찾으면 조건 검사를 건너뜀 | 오탐 0건 |
| 지난 결과를 다시 검사하면 마감 공고가 결함으로 잡힘 | 같은 재검사 | 오늘 저장소와 대조 | 받은 날이 아니면 마감 검사를 건너뜀 | 과거 결과도 재검사 가능 |
| "이거 말고"에 같은 공고를 다시 보여 주며 "다른 공고를 찾았다"고 답함 | 사람 채점 준비 중 직접 물어봄 | 서버는 무엇을 보여 줬는지 모르고, 라우터에 "더 보기" 신호가 없었다 | 앱이 본 공고를 쌓아 보내고 서버가 빼고 다음을 줌. 답은 서버가 "N건 중 6~10번째"로 씀 | 끝까지 넘겨 볼 수 있음, 케이스 2개 추가 |
| 챗봇 카드에 회사명·제목이 빈 공고 | 챗봇 결과에서 빈 카드 발견 → 저장소 전체 조회 313건(게시 중 295) | 09-11에 파서를 고치고 공고 번호만으로 상세 328건을 다시 받음. 제목·회사명·마감일은 목록 줄(`list_item`)에서 오는데 번호만 붙어 들어와 저장된 값을 빈 값으로 덮어씀. 야간 배치는 이미 있는 공고의 상세를 다시 받지 않아 계속 남음 | 목록 값이 둘 다 빈 레코드는 저장된 제목·회사명·마감일을 이어받게 함(`keep_listing_fields`, 규칙·저장소·다시 파싱 세 곳). 그 전 원본의 목록 줄로 313건 복구(백업 후) | 빈 공고 0건, 마감일 267건 되찾음, 마감 지난 19건 만료 처리 |
| 회사명에 "관심기업 등록"·"현대자동차그룹 대기업" 같은 목록 뱃지가 붙음 | 챗봇 카드 → 09-13 밤 목록 21만 6천 줄 중 2,203줄 | 회사 링크가 없는 카드는 칸 전체 글자를 읽는데, 버튼 글자만 지우고 그룹·기업형태 뱃지는 남김 | 뒤에서부터 뱃지를 떼는 `clean_company_name`을 수집기·정규화·목록 저장에 적용. "~그룹"은 떼고 법인 표기만 남으면 회사명으로 보고 되돌림("주식회사 와이앤컨설팅그룹") | 저장소 897행·목록 803행 정리, 남은 것 0 |
| 챗봇이 "AI 엔지니어"·"LLM 개발자"를 거의 못 찾음 | 평가 케이스에 없는 물음 20개로 돌려 봄 | 여러 낱말 직무를 한 덩어리로 LIKE 검색 | 제목에서 낱말로 나눠 찾고 끝말(개발자·엔지니어)과 표기(AI↔인공지능)를 넓힘. 태그·본문은 말 그대로만(나눠 찾으면 772→2,956건으로 헐거워짐) | 판교 AI 엔지니어 신입 1→50, LLM 개발자 2→104, iOS 개발자 경력 9→143 |
| "대기업 IT 신입"에 "(대기업 상주)" 파견 공고 | 같은 물음 | 기업형태 말을 공고 글에서 찾음 | 기업 정보 칸(`company_type`)으로 거름. "1000대기업" 제외 | 실제 대기업·공기업 공고만 |
| 같은 공고가 지역만 바뀌어 목록 다섯 칸을 채움 | 같은 물음 | 파견 회사가 지역별로 따로 올림 | 회사·머리말 뗀 제목·경력·고용형태가 같으면 한 건 | 게시 중 329묶음 700건이 묶임 |
| 제목에 `&amp;`가 그대로 | 같은 물음 | 사이트가 제목 속성에 `&`를 두 번 감쌈 | 수집·정규화·목록 저장에서 HTML 기호를 풂, 기존 데이터 정리(백업 후) | 공고 1,840건·목록 2,905건 |
| "개발"로 찾으면 "사업개발" 공고 | 같은 물음 | 글자 포함 검색 | 사업·상품·교육·영업·조직·인재 뒤의 "개발"은 제외 | — |
| "추천 10초대"가 실제와 다름 | 단계별 시간 로그 | 기록 없이 눈으로 본 값 | 단계마다 시간을 로그와 응답에 남김 | 실측 중앙값 18.6초, 병목은 재정렬 |

## 5. 한계와 다음 단계

**평가의 한계**

- 표본이 작다(회차당 43~50건, 이력서 5종). 큰 변화만 잡는다.
- 채점자가 한 명이다. 채점자 간 일치도는 재지 않았다.
- 규칙 결함 검사는 2026-09-13 전에는 건수를 남기지 않았다. 그 전의 결과 파일은 이력서 조건이 같이 저장되지 않아 과거 수치로 쓰지 않는다.
- 챗봇 답 문장 2차 채점 뒤 고친 것(키워드·지역·마감 시각·문구)은 사람이 다시 매기지 않았다.
- 응답 시간은 한 번(10회) 쟀다. 시간대·OpenAI 부하에 따라 달라질 수 있다.

**다음 단계**

| 할 일 | 이유 |
|---|---|
| 판정 흔들림 줄이기 검토 | 같은 공고의 등급이 12% 바뀐다. 회차 비교가 이 흔들림에 묻힌다 |
| 새 이력서 묶음으로 사람 채점 | 평가 전용 5종도 두 회차 썼다. 조건을 통과하는 후보가 12건을 넘는 이력서가 있어야 사전 순위를 잴 수 있다 |
| 마감 확인을 재정렬과 동시에 | 순서대로 기다리는 2.6초를 줄일 수 있다 |
| 재정렬 시간 줄이기 검토 | 전체의 60%다. 건수·모델·추론 강도는 적합도와 같이 재야 한다 |
| 채점자 한 명 더(겹치는 20건) | 채점자 간 일치도가 없다 |
| 챗봇 케이스에 흔들린 칸 검사 더하기 | 범위 밖 번호 뒤 조건, "없는 정보"와 직전 답 가리킴이 겹칠 때를 안 본다 |
| 챗봇 3차 사람 채점(새 물음 섞기) | 2차 뒤 고친 것을 사람이 다시 읽지 않았다. 2차의 새 물음 10개는 케이스로 옮긴다 |
